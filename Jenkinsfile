pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Jenkins build the project'
            }
        }
        stage('Test') {
            steps {
                echo 'Jenkins test the project'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Jenkins deploy the project'
            }
        }
    }
}
